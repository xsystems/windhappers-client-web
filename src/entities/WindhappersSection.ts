export interface WindhappersSection {
  content: string;
  media: {
    formats: {
      alternativeText: string;
      caption: string;
      medium: {
        url: string;
      };
    };
  };
  title: string;
}
