export interface FlickrPhoto {
  description: {
    _content: string;
  };
  farm: number;
  id: string;
  originalformat: string;
  originalsecret: string;
  secret: string;
  server: string;
  title: {
    _content: string;
  };
}
